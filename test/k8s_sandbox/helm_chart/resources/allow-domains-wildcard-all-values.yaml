# Pinned to cilium: this fixture exercises Cilium-only policy features.
networkPolicyProvider: cilium
services:
  default:
    image: "python:3.12-bookworm"
# "*" means allow all domains; the chart must not emit serverNames (which has no
# allow-all form and would be rejected by Cilium's CRD validation).
allowDomains:
  - "*"
