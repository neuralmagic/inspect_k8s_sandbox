# Pinned to cilium: this integration test exercises Cilium-only DNS/FQDN egress.
networkPolicyProvider: cilium
services:
  default:
    resources:
      limits:
        memory: "128Mi"
        cpu: "100m"
      requests:
        memory: "128Mi"
        cpu: "100m"
allowEntities:
  - world
